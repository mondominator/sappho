/**
 * Placeholder migration - reserved number
 *
 * Migration number 018 was skipped during development.
 * This placeholder exists to document the gap and prevent
 * accidental reuse of this migration number.
 */

function up(_db) {
  return Promise.resolve();
}

function down(_db) {
  return Promise.resolve();
}

module.exports = { up, down };
